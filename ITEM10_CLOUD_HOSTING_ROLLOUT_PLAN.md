# 第十项：微信云托管上线、安全与备份计划

> 状态：云托管登录、现有代理图片上传和菜谱保存已由用户验证可用；OSS 直传作为备选暂停，当前进入阶段 B 的生产数据与安全基线。  
> 范围：将现有微信小程序和 Spring Boot 后端部署到可供真实用户访问的环境，并为后续健身 Agent 预留扩展边界。  
> 原则：R1～R7 及原第四～第九项均已验收，不重做其业务逻辑；第十项仍按“小步实施 → 自动化测试 → 用户验收 → 再继续”推进。

## 1. 方案结论

第一选择为**微信云托管**，不把现有 Java 后端重写成云函数：

- Spring Boot 后端以 Docker 容器部署，继续使用现有 REST 接口和 MySQL 数据模型。
- 小程序生产环境通过 `wx.cloud.callContainer` 调用云托管服务；本地开发继续使用 `wx.request`，两种传输共用同一套业务 API 封装。
- 生产数据库使用独立的云 MySQL，不把数据库或持久文件放在应用容器内。
- 第一阶段保留阿里云 OSS，先验证云托管到 OSS 的出站访问和小程序图片域名；是否迁移到 CloudBase 存储或 COS 另行决策。
- 先进行香港及实际目标境外地区的真机网络验收，再决定是否需要额外购买香港／新加坡服务器和自有域名。云托管免配传统服务器域名，不等于对所有境外网络都自动保证低延迟和高可用。

参考资料：

- [微信云托管产品说明与计费](https://cloud.tencent.com/document/product/876/113602)
- [云托管服务管理与 Dockerfile 部署](https://cloud.tencent.com/document/product/876/46901)
- [Java 应用部署说明](https://cloud.tencent.com/document/product/1243/49239)
- [小程序通过 wx.cloud.callContainer 访问云托管](https://docs.cloudbase.net/run/develop/access/mini)
- [云托管公网访问说明](https://docs.cloudbase.net/run/deploy/networking/public)

## 2. 与“原第十项”的关系

本计划**就是原第十项“部署、安全与备份”的具体落地路线**，不是依赖于第十项完成后才能开始的第十一项。

- 云托管 PoC、Docker 化、健康检查和测试环境部署可以作为第十项的前半段先做。
- 正式发布依赖第十项中的生产密钥、云数据库迁移、备份恢复、监控告警、定时任务单实例保障、回滚方案全部通过验收。
- 健身 Agent 的接口设计可以并行规划；Agent 正式接触真实用户数据，必须等身份认证、数据权限、密钥管理、备份恢复和隐私边界稳定后再上线。

## 3. 分阶段执行计划

### 阶段 A：只做云托管 PoC

1. 开通与当前小程序 AppID 关联的微信云托管测试环境。
2. 为后端增加生产 Dockerfile、`.dockerignore` 和独立健康检查接口。
3. 增加 `local`／`cloud` 环境配置；清除生产包中的本机 Bonjour 地址依赖。
4. 前端 API 层增加双传输适配：本地走 `wx.request`，云环境走 `wx.cloud.callContainer`；业务页面不直接判断环境。
5. 使用测试数据库部署单实例测试版本，验证登录、菜谱、训练、能量、R5、R6、R7 和图片链路。
6. PoC 验收后暂停，由用户决定是否进入生产化阶段。

阶段 A 的逐步操作、环境变量和验收清单见 `backend/CLOUD_HOSTING_POC.md`。首次云库初始化使用受保护脚本 `backend/deployment/bootstrap-cloud-poc.sh`：仅允许空库，跳过已并入 `database.sql` 的重复迁移，完成后删除本地测试账号并核对结构及种子数据。部署上传包由 `backend/deployment/package-cloud-poc.sh` 生成，不包含 `.env`、本地数据或历史迁移文件。

### 阶段 B：生产数据与安全基线

当前子阶段：补齐生产模式启动校验。除既有微信认证和 Token 校验外，生产启动还必须拒绝本机/缺失数据库配置、缺失或非 HTTPS 的 OSS 配置、开启 R7 合成样本，以及启用真实匿名快照却缺少独立强 HMAC 密钥。现有代理上传不要求 STS 角色。

进度更新（2026-09-20）：生产配置启动校验已由用户验收通过；用户决定跳过数据库与 OSS 最小权限复核。当前执行日志与告警配置，完成后再进入实例和定时任务策略。

1. 创建生产云 MySQL，使用最小权限业务账号，并限制网络访问范围。
2. 按 `backend/migrations/` 文件顺序在空库演练，再迁移正式数据；迁移前后核对表、索引、约束和关键记录数。
3. 通过云环境变量／密钥管理注入数据库密码、微信 AppSecret、Token 签名密钥、OSS RAM 凭证及 R7 HMAC 密钥；任何真实密钥不得进入代码、镜像、日志或计划文档。
4. 正式环境必须设置：
   - `FITNESS_WECHAT_MOCK_ENABLED=false`
   - `FITNESS_COMPARISON_INCLUDE_SYNTHETIC=false`
   - 固定且至少 32 字节的 `FITNESS_AUTH_TOKEN_SECRET`
5. OSS 使用最小权限 RAM 身份，验证上传、读取、删除和跨域／合法域名配置。
6. 配置结构化日志、慢请求告警、登录上游失败告警和资源使用告警；继续禁止记录登录 code、Token、AppSecret、openid、健康明细和请求正文。

### 阶段 C：多实例安全、备份与恢复

1. 当前审计清理和 R7 快照都使用 `@Scheduled`。在允许多个实例前，为任务增加分布式锁，或拆成只运行一个副本的定时任务服务，避免重复执行。
2. 初始建议应用规格为 1 核 2 GB，生产最小实例数 1、最大 2～3；数据库连接池按每个实例约 5 条起步，再用压测结果调整。
3. 开启 MySQL 自动备份和保留策略；另做一次手工备份与校验。
4. 在隔离环境执行完整恢复演练，记录恢复耗时、恢复点和校验 SQL。只有“备份可成功恢复”才算备份完成。
5. 配置版本灰度、快速回滚和数据库迁移回退原则；不可逆迁移必须采用向前兼容的分阶段发布。

### 阶段 D：正式发布与境外验收

1. 发布体验版，分别在中国大陆、香港和实际目标境外地区使用真机测试。
2. 验证首次登录、再次进入自动登录、Token 过期重登、微信运动授权／拒绝／同步、图片上传、核心数据增删改查。
3. 记录登录及核心接口的成功率、P50/P95 延迟和图片加载情况。
4. 若目标地区表现达标，继续使用微信云托管；若明显不达标，再评估香港／新加坡独立后端、自有 HTTPS 域名和跨地域数据库方案，避免提前购买闲置资源。

## 4. 微信登录现状审查

### 已符合的核心设计

当前实现已经满足同一小程序内的“微信用户使用当前微信账号即可注册登录”：

1. 小程序启动时调用 `wx.login` 获取一次性 code。
2. `POST /api/user/login` 是唯一免业务 Token 的接口。
3. 后端用 AppID、AppSecret 和 code 调用微信 `jscode2session`，只在服务端取得 openid。
4. 服务端按 openid 查询 `users`；不存在时创建默认档案，存在时直接复用原账户。
5. `users.openid` 有唯一约束，业务接口使用服务端签发的 Bearer Token 识别用户，不接受客户端指定 userId。
6. code 失效时前端只重新获取并重试一次；微信密钥、code 和 sessionKey 不保存到数据库，也不写日志。

因此，用户无需另设用户名和密码；同一个微信账号在**同一个小程序 AppID** 下会进入同一项目账户。openid 是“用户 + 小程序 AppID”范围的标识：如果以后更换 AppID，当前系统不会自动识别为原账户；当前也没有使用 UnionID 做跨小程序／公众号账户合并。

### 正式上线前需要补齐

1. **首次并发登录幂等（已实现，待用户验收）**：注册由独立事务完成；同一新用户并发首登产生唯一键竞争时，冲突请求会在事务结束后重新查询并复用胜出的账户。已覆盖新用户、老用户和竞争恢复测试。
2. **云托管传输适配（已实现，待云端验收）**：`utils/api.js` 支持本地 `wx.request` 与云端 `wx.cloud.callContainer` 双传输；图片 multipart 上传在 PoC 中暂走云托管公网 HTTPS 默认域名。
3. **固定生产签名密钥**：未配置 `FITNESS_AUTH_TOKEN_SECRET` 时，每次重启都会生成临时密钥。生产必须固定、妥善保管并设计轮换方式；最低32字节要求已加入生产启动校验，密钥托管和轮换留在云托管阶段完成。
4. **生产模式校验（认证部分已实现，待用户验收）**：`FITNESS_PRODUCTION_MODE=true` 时会拒绝模拟登录、缺失 AppID/AppSecret 或弱 Token 密钥。数据库、OSS 和合成数据等非认证配置留在对应生产化阶段校验。
5. **账户生命周期**：当前具备自动注册和登录，但尚未定义账户注销／项目数据整体删除流程。正式面向真实用户前，应按隐私要求补充明确入口与后端清理策略。
6. **测试缺口（本轮范围已补齐）**：现有测试已覆盖微信响应解析、错误映射、code 单次重试、认证拦截、Controller 登录契约、新用户创建、老用户复用和首次并发登录冲突恢复。

## 5. 后续健身 Agent 的预留方案

- 将 Agent 作为独立 `fitness-agent` 服务，现有后端保持 `fitness-api` 身份与数据边界。
- Agent 只能调用经过鉴权、范围受控的后端工具接口，不直接连接业务数据库。
- 读取饮食、训练和趋势需要最小化数据；创建或修改计划等写操作必须由用户确认。
- 第一阶段调用外部模型 API，无需 GPU；可从 1 核 2 GB 独立服务起步。
- 在跨境调用模型服务前单独评估隐私告知、用户同意、数据最小化、保存期限和跨境数据要求。

## 6. 第十项完成标准

- 本地开发与云托管测试环境可切换，生产包不包含本机地址或真实密钥。
- 首次微信登录自动建户、再次登录复用账户、并发首登、401 自动重登均通过测试。
- 所有迁移在云数据库演练并核对成功，R1～R7 无回归。
- 定时任务在多实例下不会重复产生副作用。
- 自动备份已开启，且至少完成一次隔离恢复演练。
- 大陆、香港和目标境外地区真机验收通过；若不通过，有量化数据支持下一步地域部署决策。
- 灰度发布、告警、回滚和密钥轮换流程有记录且实际演练过。
