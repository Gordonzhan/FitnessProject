# 健身饮食日记小程序需求文档

**版本**：V1.0
**日期**：2025年3月
**作者**：产品经理

------

## 1. 项目背景与目标

随着健康意识的提升，越来越多的健身爱好者希望同时管理饮食摄入与运动消耗。然而，市面上多数应用将饮食记录与运动记录分开，数据割裂，难以直观看到每日热量盈亏。本项目旨在开发一款**轻量级、聚焦健身与饮食联动**的小程序，用户可通过简单的拍照记录餐食、录入训练动作，并自动计算摄入与消耗的差值，帮助用户更科学地管理健身目标。

------

## 2. 目标用户

- 健身新手：希望通过简单工具开始记录饮食与训练。
- 健身爱好者：已有一定基础，希望量化每日热量缺口或盈余。
- 减脂/增肌人群：需要精确控制热量差，并关注蛋白质等营养元素。

------

## 3. 产品范围

### 3.1 核心功能模块

- **菜谱记录**：上传食物图片，输入食材与重量，自动计算总热量及三大营养素（蛋白质、碳水、脂肪）。
- **训练记录**：按日期查看/新增训练动作（动作名称、重量、组数、次数），并估算总消耗卡路里。
- **每日对比**：展示当日摄入总热量与运动消耗热量的差值，并给出趋势提示。

### 3.2 非功能需求

- **易用性**：界面简洁，操作步骤不超过3步即可完成记录。
- **数据安全**：用户数据仅存储在本地或用户授权云存储，支持导出。
- **性能**：启动速度<2秒，计算响应<1秒。
- **兼容性**：支持iOS和Android微信小程序环境。

------

## 4. 功能需求详情

### 4.1 全局导航（底部Tab）

底部固定三个Tab：**菜谱**、**健身**、**对比**。
当前选中项高亮，默认进入“菜谱”页。

### 4.2 菜谱模块

#### 4.2.1 菜谱列表页

- 按日期分组展示已记录的菜谱（日期倒序）。
- 每个菜谱项展示：封面图、菜名（或自动生成“早餐/午餐/晚餐”标签）、总热量、蛋白质含量。
- 点击某项进入详情页，支持编辑和删除。
- 底部悬浮“+”按钮，点击进入“新增菜谱”页。

#### 4.2.2 新增/编辑菜谱页

- **菜品图片**：支持从相册选择或拍照，最多9张，首张作为封面。
- **菜谱名称**：可选填，若为空则自动根据时间生成“早餐/午餐/晚餐/加餐”。
- **食材列表**：
  - 支持逐条添加食材：名称、重量（克）、可选择预设食材（从内置食物库选择或自定义）。
  - 自动根据食物库数据计算该食材的蛋白质、碳水、脂肪及热量。
  - 食材列表实时汇总总热量及三大营养素。
- **制作步骤**：可选填文本描述。
- 保存后返回列表页，数据存入本地数据库（后续可扩展云端同步）。

#### 4.2.3 食物营养数据库（内置）

- 预置常见食物（如鸡胸肉、米饭、鸡蛋等）每100g的营养成分。
- 支持用户自定义食物并保存至本地库。

### 4.3 健身模块

#### 4.3.1 训练记录日历页

- 顶部显示日期选择器，默认选中今日。
- 下方展示选中日期的训练记录。
- 若无记录，显示“暂无训练，点击新增”按钮。

#### 4.3.2 新增训练动作页

- **动作名称**：输入或从常用动作库选择（如卧推、深蹲、硬拉等）。
- **重量**：数字输入（单位kg）。
- **组数**：数字输入。
- **次数/组**：数字输入（每组次数）。
- 支持继续添加多个动作。
- 完成录入后，系统根据内置MET（代谢当量）值及用户体重（需用户首次填写）估算该动作消耗卡路里，并累加得到本次训练总消耗。
  - 计算公式：消耗（kcal）= MET × 体重(kg) × 时间(小时)。
    时间估算：每组动作按30秒执行时间 + 组间休息时间（可预设默认值或让用户输入总时长）。
  - 简化版：允许用户直接输入“总消耗预估”作为兜底。

#### 4.3.3 训练历史查看

- 按日期展示已录训练，包含动作列表及总消耗。

### 4.4 对比模块

#### 4.4.1 今日热量对比卡片

- 展示今日摄入总热量（从菜谱模块汇总当日所有菜谱热量之和）。
- 展示今日运动消耗总热量（从健身模块汇总当日所有训练消耗之和）。
- 计算差值：摄入 - 消耗。
- 根据差值给出文字建议（如“热量盈余xx kcal，需注意控制”或“热量缺口xx kcal，继续坚持”）。
- 提供近7天趋势图表（可选，V1可先做单日对比）。

#### 4.4.2 历史对比

- 可切换日期查看历史某天的摄入与消耗对比（日期选择器）。

------

## 5. 数据模型（简化）

### 5.1 用户（本地存储）

- userId（设备唯一标识）
- 体重（kg）（用于消耗计算）
- 常用动作库（数组）

### 5.2 食物库

- foodId
- foodName
- protein（每100g）
- carb
- fat
- calorie

### 5.3 菜谱记录

- recipeId
- date
- mealType（早餐/午餐/晚餐/加餐）
- coverImage
- images（数组）
- ingredients：数组对象 { foodId/foodName, weight, protein, carb, fat, calorie }
- steps（文本）
- totalNutrients：{ protein, carb, fat, calorie }

### 5.4 训练记录

- workoutId
- date
- exercises：数组对象 { name, weight, sets, reps, met, durationMin, calories }
- totalCalories

------

## 6. 界面原型描述（文字版）

- **菜谱列表**：卡片式，左图右文字，显示热量和蛋白质。底部浮动+号。
- **新增菜谱**：分区块：图片上传区（可滑动预览）、食材列表（可展开添加项）、总营养汇总条、步骤输入区、保存按钮。
- **健身日历**：顶部日期选择（左右箭头切换），下方显示动作列表（每个动作一行，显示名称、重量、组数x次数），底部显示总消耗。新增按钮在右上角。
- **新增训练**：动作选择下拉+自定义输入，重量、组数、次数输入框，底部“添加动作”按钮，完成后显示本次预估消耗和保存。
- **对比页**：两个大卡片（摄入/消耗）并列，差值居中突出显示，下方可切换日期。

------

## 7. 开发优先级（MVP版本）

**P0（必须实现）**

- 底部Tab导航
- 菜谱增删改查，图片上传，食材录入及营养计算（基于本地食物库）
- 训练增删改查，动作录入，总消耗估算（基于预设MET和体重）
- 对比页当日差值计算

**P1（第二优先级）**

- 食物库自定义添加
- 训练动作库常用预设
- 日期切换查看历史对比
- 数据导出/导入（备份）

**P2（后续迭代）**

- 图表趋势（周/月）
- 社交分享（生成打卡图）
- 云端同步
- 拍照识别食材（AI）

------

## 8. 技术要求

- 前端：微信小程序原生框架（WXML+JS）或uni-app。
- 数据存储：微信小程序本地缓存（wx.setStorageSync）或引入轻量级数据库如PouchDB。
- 计算逻辑：前端实时计算，无需后端（MVP阶段）。

------

## 9. 风险与应对

- **营养数据准确性**：初期采用通用数据，提示用户仅供参考；后续可接入权威食物数据库API。
- **运动消耗估算误差**：说明估算逻辑，允许用户手动修正。
- **用户留存**：通过每日推送提醒记录，建立习惯。

------

## 10. 附录

- MET参考表（常见力量训练、有氧运动代谢当量）
- 食物营养成分参考来源（中国食物成分表）